# Local Vite. Copy to frontend/.env.
# Production (Vercel) must set both values in the Vercel project environment.
# VITE_API_BASE_URL must be the public HTTPS API origin, with no trailing slash.
# Do not use localhost in a production build.

VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_KEY=
